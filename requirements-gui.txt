# OpenTofu Lab Automation GUI - Enhanced Build Dependencies
# Core dependencies for building the GUI executable with advanced optimizations

# ===== CORE BUILD TOOLS =====
pyinstaller>=6.3.0                # Latest PyInstaller with improved optimizations
wheel>=0.42.0                     # Package building
setuptools>=69.0.0                # Setup tools

# ===== OPTIMIZATION TOOLS =====
# Performance optimization
psutil>=5.9.0                     # System resource monitoring and optimization
upx>=4.0.0; platform_system != "Darwin"  # UPX compression (not on macOS)

# Alternative packaging tools (optional but recommended)
nuitka>=1.8.0                     # Python compiler for better performance
cx-Freeze>=6.15.0                 # Alternative packaging tool
auto-py-to-exe>=2.34.0            # GUI wrapper for PyInstaller

# ===== GUI ENHANCEMENTS =====
pillow>=10.0.0                    # Image processing for icons and graphics
# tkinter is included with Python standard library

# ===== DEVELOPMENT AND TESTING =====
pytest>=7.4.0                     # Testing framework
black>=23.0.0                     # Code formatting
flake8>=6.0.0                     # Linting
mypy>=1.5.0                       # Type checking

# ===== DISTRIBUTION AND DEPLOYMENT =====
briefcase>=0.3.0                  # Cross-platform packaging
poetry>=1.6.0                     # Dependency management
twine>=4.0.0                      # Package uploading

# ===== PLATFORM-SPECIFIC OPTIMIZATIONS =====
# Windows-specific
pywin32>=306; platform_system == "Windows"        # Windows API access
wmi>=1.5.1; platform_system == "Windows"          # Windows Management Interface

# macOS-specific  
py2app>=0.28; platform_system == "Darwin"         # macOS app creation
dmgbuild>=1.6.0; platform_system == "Darwin"      # DMG creation

# Linux-specific
python-appimage>=1.2.0; platform_system == "Linux"  # AppImage creation

# ===== OPTIONAL PERFORMANCE ENHANCEMENTS =====
# Memory optimization
pympler>=0.9                      # Memory profiling
memory-profiler>=0.61.0           # Memory usage monitoring

# Startup time optimization
importlib-metadata>=6.8.0         # Faster imports
lazy-import>=0.2.2                # Lazy loading

# ===== DEBUGGING AND PROFILING =====
py-spy>=0.3.14                    # Python profiler
snakeviz>=2.2.0                   # Profile visualization
line-profiler>=4.1.0              # Line-by-line profiling

# ===== DOCUMENTATION =====
sphinx>=7.1.0                     # Documentation generation
sphinx-rtd-theme>=1.3.0           # ReadTheDocs theme

# ===== QUALITY ASSURANCE =====
bandit>=1.7.5                     # Security linting
safety>=2.3.0                     # Dependency vulnerability checking
pre-commit>=3.4.0                 # Git hooks

# ===== VERSION CONSTRAINTS =====
# Ensure compatibility
python-dateutil>=2.8.0
six>=1.16.0
packaging>=23.0
certifi>=2023.7.22

# ===== NOTES =====
# - tkinter is included with Python standard library (no separate installation needed)
# - Platform-specific dependencies are handled automatically
# - UPX compression requires separate UPX binary installation
# - Some tools are optional but provide significant benefits

# ===== INSTALLATION INSTRUCTIONS =====
# Basic installation:
#   pip install -r requirements-gui.txt
#
# Development installation (includes all optional tools):
#   pip install -r requirements-gui.txt[dev]
#
# For production builds (minimal dependencies):
#   pip install pyinstaller>=6.3.0 psutil>=5.9.0 pillow>=10.0.0

# ===== TROUBLESHOOTING =====
# If you encounter issues:
# 1. Update pip: python -m pip install --upgrade pip
# 2. Install build tools: pip install --upgrade setuptools wheel
# 3. For Windows: pip install --upgrade pywin32
# 4. For macOS: xcode-select --install
# 5. For Linux: sudo apt-get install python3-dev build-essential
